/**
 * Placeholder MCP handler.
 *
 * This repo currently exposes Admin API capabilities via REST routes under `/admin/*`.
 * Next step is wiring a real MCP transport layer that maps tool calls -> service calls.
 */
export function registerMcpHandlers() {
  // Intentionally minimal for now.
}
