---
description: Start the local mcp-server runtime
---

**Skills:** `.cursor/skills/eds-xwalk-block-workflow/SKILL.md` · `.cursor/skills/eds-figma-design-tokens/SKILL.md` · `.cursor/skills/eds-aem-admin-api/SKILL.md` (after server is up)

Start the runtime server used by all generation/validation/transform/admin commands.

From repo root, run:

- `cd mcp-server`
- `npm install`
- `npm run dev`

Server defaults to `http://localhost:8787` (configure with `PORT`).

