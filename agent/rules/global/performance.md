# Global Performance Rules (EDS blocks)

- Keep JS minimal; only add JS when needed for interaction.
- Avoid heavy dependencies by default.
- Prefer CSS for layout/animation when feasible.
- Ensure images are responsive (`width: 100%`, `height: auto`) and avoid forced layout.
- Keep DOM operations small and scoped to the block.
